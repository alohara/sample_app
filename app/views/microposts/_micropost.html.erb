<li>
  <span class="content">
    <%= wrap(micropost.content) %>
    <%= micropost.latitude %>
	<%= micropost.longitude %>
	<%= micropost.ip_address %>
	<%= micropost.zipcode %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago test.
    <% if current_user?(micropost.user) %>
      <div class="checkbox"> <input type="checkbox"><b>HardCode Visible</b>
	    <%= check_box_tag :visible, 1, micropost.visible ? true : false, disabled: true %>
        <%= link_to "delete", micropost, method: :delete,
		   						   confirm: "You sure, Yo?",
								   title: micropost.content %>
	  </div>
  </span>
    <% end %>
</li>